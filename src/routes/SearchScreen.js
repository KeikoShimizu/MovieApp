import React from 'react'
import SearchResults from '../tabs/search/SearchResults'

const SearchScreen = ( { navigation }) => { 
    return(
        <SearchResults navigation={navigation}/>
    )  
}

export default SearchScreen
