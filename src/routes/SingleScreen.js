import SingleMovie from "../tabs/movies/SingleMovie"

const SingleScreen = ({ navigation, route }) => {
    return(
        <SingleMovie navigation={navigation} route={route}/>
    )
}

export default SingleScreen
